// Bài 04
/**
 * Sơ đồ 3 khối
 * Input: Nhập vào chiều dài và chiều rộng của hình chữ nhật
 * var length = 37
 * var width = 22
 *
 * Process:
 * 1. Nhập vào chiều dài và chiều rộng của hình chữ nhật
 * var length = 37
 * var width = 22
 * 2. Tính diện tích và chu vi của hình chữ nhật
 * Diện tích: var S = length * width
 * Chu vi: var P = (length + width) * 2
 *
 * Output: Tính diện tích hình chữ nhật: var S = length * width
 * Tính chu vi hình chữ nhật: var P = (length + width) * 2
 */
var length = 37;
var width = 22;
var S = length * width;
var P = (length + width) * 2;
console.log("Diện tích hình chữ nhật:", S);
console.log("Chu vi hình chữ nhật:", P);
